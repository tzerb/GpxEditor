
//import * as initialState from './initialState';


export default function gpxFileReducer(state :any, action:any) {
    //alert('here');
    return ({
  gpxFile : {
    waypoints:[] as any[],
    Tracks:[] as any[]
  }
    });
}